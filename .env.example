# Fejlesztés: hagyd üresen vagy /api — a Vite proxy (vite.config.ts) továbbítja a Laravelhez.
# Production build (npm run build): kitöltve a .env.production fájlban.
# VITE_API_BASE_URL=https://api.cellauto.ro/api
